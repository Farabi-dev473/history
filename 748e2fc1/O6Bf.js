/**
 * pdaAddress -> Account Index 8
 * auctionHouse -> previous index of pdaAddress
 * tokenAddress -> Account Index 2/3
 * mintAddress  -> Account Index 4
 * seller       -> Account Index 1
 * sellerReferral -> After index 9
 */

const parseMagicEdenTx = (txId) => {
  
}