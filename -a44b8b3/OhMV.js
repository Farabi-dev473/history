const getEntities = (entities = []) => {

    entities.forEach((entity, index) => {
       entities[index] = entity.toLowerCase()
    })

    if(entities.includes("seller")) {
       
    }

    if(entities.includes("buyer")) {

    }
}